# Routes

Route modules register controllers with Express. `index.js` mounts the extracted Theme and Health routes. Existing legacy endpoints remain registered by `app.js` for backward compatibility.
