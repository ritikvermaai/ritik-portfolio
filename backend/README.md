# Backend

The Express/Mongoose API lives at the repository root in `server.js` so the original deployment shape remains easy to run. This directory is reserved for future backend modules/migrations.

The backend keeps the original MongoDB model names and REST endpoints.
