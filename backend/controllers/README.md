# Controllers

Controllers translate HTTP requests into service calls. Theme and health are fully extracted; remaining compatibility endpoints can be migrated one domain at a time without changing their public URLs.
