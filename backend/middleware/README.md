# Middleware

Authorization, CSRF, upload validation and other request-level security concerns belong here. The existing application keeps its compatibility middleware while new extracted modules can be migrated without changing public API URLs.
