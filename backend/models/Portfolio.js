// Logical Portfolio domain model. Reuses PortfolioContent so existing MongoDB
// data remains untouched while the codebase gains a domain-level entry point.
module.exports = require('./PortfolioContent');
